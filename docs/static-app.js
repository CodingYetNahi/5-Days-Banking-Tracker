const $=s=>document.querySelector(s),esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let updates=[];function card(p){return `<article class="card"><p class="meta">${p.date} · ${p.type}</p><h3><a href="${p.url}" target="_blank" rel="noopener">${esc(p.title)}</a></h3><p>${esc(p.summary)}</p><small>Source: ${esc(p.source)}</small></article>`}async function init(){document.documentElement.dataset.theme=localStorage.getItem('theme')||'light';$('#theme')?.addEventListener('click',()=>{const x=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=x;localStorage.setItem('theme',x)});updates=await fetch('updates.json').then(r=>r.json());if(location.pathname.endsWith('blog.html')){$('#posts').innerHTML=updates.map(card).join('');return}const draw=()=>{const x=TimerUtils.elapsedParts('2024-09-04T10:00:00+05:30');$('#timer').innerHTML=['years','months','days','hours','minutes','seconds'].map(k=>`<span class="time-part"><strong>${x[k]}</strong><small>${k}</small></span>`).join('');$('#days-total').textContent=`${x.totalDays} days elapsed since 4 September 2024, 10:00 am IST`};draw();setInterval(draw,1000);$('#status').textContent='Monitoring — not officially implemented';$('#latest').innerHTML=updates.slice(0,3).map(card).join('');$('#milestones').innerHTML='<article class="card"><h3>Current status</h3><p>No official implementation notification has been recorded.</p></article>';$('#timeline').innerHTML=updates.slice(0,3).map(card).join('')}init();
