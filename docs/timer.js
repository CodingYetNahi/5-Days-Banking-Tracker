function elapsedParts(start,end=new Date()){const a=new Date(start),b=new Date(end);if(!Number.isFinite(a.getTime())||!Number.isFinite(b.getTime())||b<a)return{years:0,months:0,days:0,hours:0,minutes:0,seconds:0,totalDays:0};let c=new Date(a),years=0,months=0;for(;;){const n=new Date(c);n.setUTCFullYear(n.getUTCFullYear()+1);if(n>b)break;c=n;years++}for(;;){const n=new Date(c);n.setUTCMonth(n.getUTCMonth()+1);if(n>b)break;c=n;months++}const seconds=Math.floor((b-c)/1000);return{years,months,days:Math.floor(seconds/86400),hours:Math.floor(seconds%86400/3600),minutes:Math.floor(seconds%3600/60),seconds:seconds%60,totalDays:Math.floor((b-a)/86400000)}}window.TimerUtils={elapsedParts};