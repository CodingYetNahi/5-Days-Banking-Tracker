function elapsedParts(start,end=new Date()){const a=new Date(start),b=new Date(end);let c=new Date(a),y=0,m=0;while(new Date(c).setUTCFullYear(c.getUTCFullYear()+1)<=b){c.setUTCFullYear(c.getUTCFullYear()+1);y++}while(new Date(c).setUTCMonth(c.getUTCMonth()+1)<=b){c.setUTCMonth(c.getUTCMonth()+1);m++}const s=Math.floor((b-c)/1000);return{years:y,months:m,days:Math.floor(s/86400),hours:Math.floor(s%86400/3600),minutes:Math.floor(s%3600/60),seconds:s%60,totalDays:Math.floor((b-a)/86400000)}}window.TimerUtils={elapsedParts};
